# 4-to-1 MUX (coding drill)

Quick-fire coding drill: spec to Verilog directly, no diagram. Note: select ordering here is
`{s0,s1}` (s0 as MSB) — a valid but non-default convention, worth being explicit about when
naming signals in real code to avoid ambiguity for future readers.
